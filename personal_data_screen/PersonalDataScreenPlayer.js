import React from 'react';
import { View, Text } from 'react-native';

const PersonalDataScreenPlayer = () => {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Players</Text>
    </View>
  );
};

export default PersonalDataScreenPlayer;
